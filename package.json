{
  "name": "gemini-proxy",
  "version": "1.0.0",
  "description": "Standalone Gemini Web Proxy — reverse-engineered Gemini web API with OpenAI-compatible endpoints",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
