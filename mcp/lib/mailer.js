import nodemailer from 'nodemailer';
const REQUIRED=['SMTP_HOST','SMTP_USER','SMTP_PASS','SMTP_FROM_EMAIL','SMTP_FROM_NAME'];
function requiredString(c,k){const v=c?.[k];if(typeof v!=='string'||!v.trim())throw new TypeError(`${k} must be a non-empty string`);return v.trim();}
function parsePort(v){const n=typeof v==='string'&&v.trim()?Number(v):v;if(!Number.isInteger(n)||n<1||n>65535)throw new TypeError('SMTP_PORT must be an integer between 1 and 65535');return n;}
function parseSecure(v){if(typeof v==='boolean')return v;if(typeof v==='string'){v=v.trim().toLowerCase();if(v==='true')return true;if(v==='false')return false;}throw new TypeError('SMTP_SECURE must be true or false');}
function mailbox(v,label){if(typeof v!=='string'||v!==v.trim()||/[\x00-\x1f\x7f]/.test(v)||!/^[^\s@,;<>]+@[^\s@,;<>]+\.[^\s@,;<>]+$/.test(v))throw new TypeError(`${label} must be one valid email address`);return v;}
function header(v,label){if(/[\x00-\x1f\x7f]/.test(v))throw new TypeError(`${label} contains invalid header characters`);return v;}
function formatFrom(n,e){return `"${n.replaceAll('\\','\\\\').replaceAll('"','\\"')}" <${e}>`;}
function html(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');}
const sentResult=info=>info?.messageId===undefined?{sent:true}:{sent:true,messageId:info.messageId};
export function createMailer(config,options={}){
 const p={};for(const k of REQUIRED)p[k]=requiredString(config,k);p.SMTP_PORT=parsePort(config?.SMTP_PORT);p.SMTP_SECURE=parseSecure(config?.SMTP_SECURE);mailbox(p.SMTP_FROM_EMAIL,'SMTP_FROM_EMAIL');header(p.SMTP_FROM_NAME,'SMTP_FROM_NAME');
 const opts={host:p.SMTP_HOST,port:p.SMTP_PORT,secure:p.SMTP_SECURE,auth:{user:p.SMTP_USER,pass:p.SMTP_PASS}};const transport=options.transport??(options.createTransport??nodemailer.createTransport)(opts);if(!transport||typeof transport.sendMail!=='function')throw new TypeError('SMTP transport must provide sendMail');
 return Object.freeze({
  async sendTrialOtp({to,otp,expiresMinutes}={}){mailbox(to,'to');if(typeof otp!=='string'||!/^\d{6}$/.test(otp))throw new TypeError('otp must be exactly six digits');if(typeof expiresMinutes!=='number'||!Number.isFinite(expiresMinutes)||expiresMinutes<=0)throw new TypeError('expiresMinutes must be a positive number');const text=`Kode verifikasi trial Gen Console Anda: ${otp}\n\nKode ini berlaku selama ${expiresMinutes} menit. Jangan bagikan kode ini kepada siapa pun.`;const html=`<p>Kode verifikasi trial Gen Console Anda:</p><p><strong>${otp}</strong></p><p>Kode ini berlaku selama ${expiresMinutes} menit. Jangan bagikan kode ini kepada siapa pun.</p>`;return sentResult(await transport.sendMail({from:formatFrom(p.SMTP_FROM_NAME,p.SMTP_FROM_EMAIL),to,subject:'Kode Verifikasi Trial Gen Console',text,html}))},
  async sendPaidAccess({to,password,otp,otpExpiresMinutes=15,orderCode,newExpiresAt}={}){mailbox(to,'to');if(typeof password!=='string'||password.length<10||password.length>200||/[\x00-\x20\x7f]/.test(password))throw new TypeError('password is invalid');if(typeof otp!=='string'||!/^\d{6}$/.test(otp))throw new TypeError('otp must be exactly six digits');if(!Number.isInteger(otpExpiresMinutes)||otpExpiresMinutes<1||otpExpiresMinutes>60)throw new TypeError('otpExpiresMinutes is invalid');if(typeof orderCode!=='string'||!/^[A-Za-z0-9-]{8,80}$/.test(orderCode))throw new TypeError('orderCode is invalid');const activeUntil=new Date(newExpiresAt);if(!Number.isFinite(activeUntil.getTime()))throw new TypeError('newExpiresAt is invalid');const until=activeUntil.toLocaleString('id-ID',{timeZone:'Asia/Jakarta'}),text=`Pembayaran Gen Console berhasil.\n\nEmail login: ${to}\nPassword baru: ${password}\nOTP login sekali pakai: ${otp}\n\nOTP berlaku ${otpExpiresMinutes} menit. Masa aktif akun sampai ${until}.\n\nMasuk di https://gen.azkazamdigital.com dan gunakan password baru atau OTP. Periksa folder Spam jika email ini tidak terlihat di Inbox. Jangan bagikan kredensial ini.\n\nKode order: ${orderCode}`,body={to:html(to),password:html(password),otp:html(otp),minutes:html(otpExpiresMinutes),until:html(until),orderCode:html(orderCode)},htmlBody=`<h2>Pembayaran Gen Console berhasil</h2><p>Email login: <strong>${body.to}</strong></p><p>Password baru: <strong>${body.password}</strong></p><p>OTP login sekali pakai: <strong>${body.otp}</strong></p><p>OTP berlaku ${body.minutes} menit. Masa aktif akun sampai ${body.until}.</p><p>Masuk di <a href="https://gen.azkazamdigital.com">Gen Console</a> dan gunakan password baru atau OTP. Periksa folder Spam jika email ini tidak terlihat di Inbox. Jangan bagikan kredensial ini.</p><p>Kode order: ${body.orderCode}</p>`;return sentResult(await transport.sendMail({from:formatFrom(p.SMTP_FROM_NAME,p.SMTP_FROM_EMAIL),to,subject:'Akses Gen Console Anda',text,html:htmlBody}))}
 });
}
